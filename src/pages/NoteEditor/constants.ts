export const EDITOR = {
  WIDTH: 415,
  MIN_HEIGHT: 400,
  PADDING: 24,
} as const

export const TEXT = {
  FONT_SIZE: 16,
  LINE_HEIGHT: 24,
  CHAR_WIDTH: 9.6,
  FONT_FAMILY: 'ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace',
} as const

export const MENTIONS = {
  WIDTH: 250,
  MAX_HEIGHT: 200,
  ITEM_HEIGHT: 40,
  OFFSET: 8,
  Z_INDEX: 50,
} as const
